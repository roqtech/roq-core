import { FileResponseType } from '../../../platformClient/platformSpaceClient/types';
export declare type FileAssociationType = {
    createdAt: string;
    entityIdentifier: string;
    entityName: string;
    file: FileResponseType;
    fileId: string;
    id: string;
    updatedAt: string;
};
